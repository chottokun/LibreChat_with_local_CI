# LibreChat本体のHTTPS化（ポート443）
:443, blue-two.local:443 {
    reverse_proxy librechat:3080
    
    # 自己署名証明書の自動生成（検証用）
    tls internal
}

# Sandpack BundlerのHTTPS化（ポート8443）
:8443, blue-two.local:8443 {
    reverse_proxy sandpack-bundler:80
    
    # 自己署名証明書の自動生成（検証用）
    tls internal
}
